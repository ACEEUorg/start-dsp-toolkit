# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Production
/build
/dist

# Generated data files (built from markdown at deploy time)
/public/data/

# Generated translation JS files (built from YAML at deploy time)
/src/i18n/translations/*.js

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/data/

# Local junk — the admin's "Push to GitHub" sync runs `git add -A` on the
# server, so anything untracked would otherwise get committed and published
cookies.txt
*~
